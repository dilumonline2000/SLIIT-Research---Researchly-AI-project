[build]
builder = "dockerfile"
dockerfilePath = "services/module3-data/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
